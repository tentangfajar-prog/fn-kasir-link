# Commit Message Guide

Format:

```text
type(scope): message
```

Example:
```text
feat(warung): implement POS checkout
fix(permission): block kasir from brilink route
docs(prd): add acceptance criteria
```
